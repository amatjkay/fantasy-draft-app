# 🏒 ПОЛНЫЙ ТЕХНИЧЕСКИЙ ПРОМПТ ДЛЯ ФЕНТЕЗИ-ХОККЕЙ ПРОЕКТА (10/10)

## МАКСИМАЛЬНО ДЕТАЛИЗИРОВАННАЯ СПЕЦИФИКАЦИЯ С КОДАМИ И ТЕХНИЧЕСКИМИ ОСОБЕННОСТЯМИ

***

## 📐 ДИЗАЙН-СИСТЕМА

### ЦВЕТОВАЯ ПАЛИТРА (ТОЧНЫЕ HEX/RGB КОДЫ)

```css
:root {
  /* === ОСНОВНЫЕ ЦВЕТА === */
  --color-primary-bg: #FFFFFF;              /* Основной фон */
  --color-secondary-bg: #F8F9FA;            /* Вторичный фон (таблицы, карточки) */
  --color-tertiary-bg: #F1F3F5;             /* Третичный фон (hover состояния) */
  --color-header-bg: #1A1D1F;               /* Темный header/footer */
  --color-sidebar-bg: #FAFBFC;              /* Sidebar фон */
  
  /* === ТЕКСТ === */
  --color-text-primary: #212529;            /* Основной текст (черный) */
  --color-text-secondary: #6C757D;          /* Вторичный текст (серый) */
  --color-text-muted: #ADB5BD;              /* Приглушенный текст */
  --color-text-light: #868E96;              /* Светлый текст */
  --color-text-inverse: #FFFFFF;            /* Белый текст на темном */
  
  /* === ГРАНИЦЫ === */
  --color-border-light: #E9ECEF;            /* Светлая граница */
  --color-border-medium: #DEE2E6;           /* Средняя граница */
  --color-border-dark: #CED4DA;             /* Темная граница */
  --color-divider: #F1F3F5;                 /* Разделитель */
  
  /* === АКЦЕНТНЫЕ ЦВЕТА === */
  --color-primary: #0066CC;                 /* Основной синий (ссылки, кнопки) */
  --color-primary-hover: #0052A3;           /* Hover для primary */
  --color-primary-light: #E6F2FF;           /* Светлый вариант primary */
  
  --color-secondary: #5C7CFA;               /* Вторичный акцент */
  --color-secondary-hover: #4C6EF5;         /* Hover для secondary */
  
  /* === СТАТУСЫ === */
  --color-success: #28A745;                 /* Успех/доступно */
  --color-success-light: #D4EDDA;           /* Светлый успех */
  --color-success-bg: #F0F9F3;              /* Фон успеха */
  
  --color-warning: #FD7E14;                 /* Предупреждение */
  --color-warning-light: #FFF3CD;           /* Светлое предупреждение */
  --color-warning-bg: #FFF8E6;              /* Фон предупреждения */
  
  --color-danger: #DC3545;                  /* Ошибка/недоступно */
  --color-danger-light: #F8D7DA;            /* Светлая ошибка */
  --color-danger-bg: #FEF5F6;               /* Фон ошибки */
  
  --color-info: #17A2B8;                    /* Информация */
  --color-info-light: #D1ECF1;              /* Светлая информация */
  
  /* === СПЕЦИАЛЬНЫЕ === */
  --color-drafted: #868E96;                 /* Уже выбранный игрок */
  --color-your-pick: #FFD43B;               /* Ваш выбор */
  --color-timer-warning: #FF6B6B;           /* Таймер заканчивается */
  
  /* === ТЕНИ === */
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 
               0 2px 4px -1px rgba(0, 0, 0, 0.06);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 
               0 4px 6px -2px rgba(0, 0, 0, 0.05);
  --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 
               0 10px 10px -5px rgba(0, 0, 0, 0.04);
  
  /* === Z-INDEX СЛОИ === */
  --z-base: 1;
  --z-dropdown: 1000;
  --z-sticky: 1020;
  --z-fixed: 1030;
  --z-modal-backdrop: 1040;
  --z-modal: 1050;
  --z-popover: 1060;
  --z-tooltip: 1070;
  
  /* === РАДИУСЫ === */
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;
  --radius-xl: 12px;
  --radius-round: 50%;
  
  /* === ИНТЕРВАЛЫ === */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 12px;
  --spacing-lg: 16px;
  --spacing-xl: 24px;
  --spacing-2xl: 32px;
  --spacing-3xl: 48px;
  
  /* === ПЕРЕХОДЫ === */
  --transition-fast: 150ms ease-in-out;
  --transition-base: 200ms ease-in-out;
  --transition-slow: 300ms ease-in-out;
}
```


***

## 📝 ТИПОГРАФИЧЕСКАЯ СИСТЕМА

```css
/* === BASE TYPOGRAPHY === */
html {
  font-size: 16px;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 
               'Helvetica Neue', 'Arial', sans-serif, 'Apple Color Emoji', 
               'Segoe UI Emoji', 'Segoe UI Symbol';
  font-size: 14px;
  line-height: 1.5;
  color: var(--color-text-primary);
  background-color: var(--color-primary-bg);
}

/* === ЗАГОЛОВКИ === */
h1, .h1 {
  font-size: 28px;
  font-weight: 600;
  line-height: 1.2;
  letter-spacing: -0.02em;
  margin: 0 0 var(--spacing-lg) 0;
  color: var(--color-text-primary);
}

h2, .h2 {
  font-size: 22px;
  font-weight: 600;
  line-height: 1.3;
  letter-spacing: -0.01em;
  margin: 0 0 var(--spacing-md) 0;
  color: var(--color-text-primary);
}

h3, .h3 {
  font-size: 18px;
  font-weight: 600;
  line-height: 1.4;
  margin: 0 0 var(--spacing-md) 0;
  color: var(--color-text-primary);
}

h4, .h4 {
  font-size: 16px;
  font-weight: 600;
  line-height: 1.4;
  margin: 0 0 var(--spacing-sm) 0;
  color: var(--color-text-primary);
}

h5, .h5 {
  font-size: 14px;
  font-weight: 600;
  line-height: 1.4;
  margin: 0 0 var(--spacing-sm) 0;
  color: var(--color-text-secondary);
}

/* === ТЕКСТОВЫЕ КЛАССЫ === */
.text-xs { font-size: 11px; line-height: 1.3; }
.text-sm { font-size: 12px; line-height: 1.4; }
.text-base { font-size: 14px; line-height: 1.5; }
.text-lg { font-size: 16px; line-height: 1.5; }
.text-xl { font-size: 18px; line-height: 1.5; }
.text-2xl { font-size: 22px; line-height: 1.3; }

.text-normal { font-weight: 400; }
.text-medium { font-weight: 500; }
.text-semibold { font-weight: 600; }
.text-bold { font-weight: 700; }

.text-uppercase { text-transform: uppercase; letter-spacing: 0.5px; }

/* === ЦВЕТНЫЕ ТЕКСТОВЫЕ КЛАССЫ === */
.text-primary { color: var(--color-text-primary); }
.text-secondary { color: var(--color-text-secondary); }
.text-muted { color: var(--color-text-muted); }
.text-success { color: var(--color-success); }
.text-warning { color: var(--color-warning); }
.text-danger { color: var(--color-danger); }
.text-info { color: var(--color-info); }
```


***

## 🎨 КОМПОНЕНТНАЯ СИСТЕМА

### 1. ТАБЛИЦА ИГРОКОВ (CORE КОМПОНЕНТ)

```html
<div class="player-table-wrapper">
  <!-- Фильтры и поиск -->
  <div class="table-controls">
    <div class="search-box">
      <input type="search" 
             class="search-input" 
             placeholder="Поиск по имени игрока...">
      <svg class="search-icon" width="16" height="16">
        <use href="#icon-search"></use>
      </svg>
    </div>
    
    <div class="filter-group">
      <button class="filter-btn active" data-position="all">Все</button>
      <button class="filter-btn" data-position="C">C</button>
      <button class="filter-btn" data-position="LW">LW</button>
      <button class="filter-btn" data-position="RW">RW</button>
      <button class="filter-btn" data-position="D">D</button>
      <button class="filter-btn" data-position="G">G</button>
    </div>
    
    <div class="sort-dropdown">
      <select class="sort-select">
        <option value="name">По имени</option>
        <option value="salary-desc">По зарплате ↓</option>
        <option value="salary-asc">По зарплате ↑</option>
        <option value="points">По очкам</option>
      </select>
    </div>
  </div>

  <!-- Таблица -->
  <div class="table-container">
    <table class="player-table">
      <thead class="table-header">
        <tr>
          <th class="col-status"></th>
          <th class="col-player sortable" data-sort="name">
            Игрок
            <svg class="sort-icon" width="12" height="12">
              <use href="#icon-sort"></use>
            </svg>
          </th>
          <th class="col-position">Поз.</th>
          <th class="col-team">Команда</th>
          <th class="col-salary sortable" data-sort="salary">
            Зарплата
            <svg class="sort-icon" width="12" height="12">
              <use href="#icon-sort"></use>
            </svg>
          </th>
          <th class="col-stat">GP</th>
          <th class="col-stat">G</th>
          <th class="col-stat">A</th>
          <th class="col-stat">PTS</th>
          <th class="col-action"></th>
        </tr>
      </thead>
      <tbody class="table-body">
        <!-- Доступный игрок -->
        <tr class="player-row" data-player-id="1" data-status="available">
          <td class="col-status">
            <span class="status-dot status-available"></span>
          </td>
          <td class="col-player">
            <div class="player-info">
              <span class="player-name">Connor McDavid</span>
              <span class="player-meta">27 лет • Edmonton</span>
            </div>
          </td>
          <td class="col-position">
            <span class="position-badge position-c">C</span>
          </td>
          <td class="col-team">
            <span class="team-abbr">EDM</span>
          </td>
          <td class="col-salary">
            <span class="salary-value">$12,500,000</span>
          </td>
          <td class="col-stat">82</td>
          <td class="col-stat">32</td>
          <td class="col-stat">68</td>
          <td class="col-stat"><strong>100</strong></td>
          <td class="col-action">
            <button class="btn-draft btn-primary">
              <svg width="14" height="14"><use href="#icon-plus"></use></svg>
              Выбрать
            </button>
          </td>
        </tr>
        
        <!-- Уже выбранный игрок -->
        <tr class="player-row" data-player-id="2" data-status="drafted">
          <td class="col-status">
            <span class="status-dot status-drafted"></span>
          </td>
          <td class="col-player">
            <div class="player-info">
              <span class="player-name">Nathan MacKinnon</span>
              <span class="player-meta">28 лет • Colorado</span>
            </div>
          </td>
          <td class="col-position">
            <span class="position-badge position-c">C</span>
          </td>
          <td class="col-team">
            <span class="team-abbr">COL</span>
          </td>
          <td class="col-salary">
            <span class="salary-value">$12,600,000</span>
          </td>
          <td class="col-stat">82</td>
          <td class="col-stat">51</td>
          <td class="col-stat">89</td>
          <td class="col-stat"><strong>140</strong></td>
          <td class="col-action">
            <div class="drafted-by">
              <svg width="12" height="12"><use href="#icon-lock"></use></svg>
              Team Alpha
            </div>
          </td>
        </tr>
      </tbody>
    </table>
  </div>
</div>
```

```css
/* === ТАБЛИЦА ИГРОКОВ === */
.player-table-wrapper {
  background: var(--color-primary-bg);
  border: 1px solid var(--color-border-medium);
  border-radius: var(--radius-lg);
  overflow: hidden;
}

.table-controls {
  display: flex;
  align-items: center;
  gap: var(--spacing-md);
  padding: var(--spacing-lg);
  background: var(--color-secondary-bg);
  border-bottom: 1px solid var(--color-border-light);
  flex-wrap: wrap;
}

.search-box {
  position: relative;
  flex: 1;
  min-width: 200px;
}

.search-input {
  width: 100%;
  height: 36px;
  padding: 0 var(--spacing-md) 0 36px;
  border: 1px solid var(--color-border-medium);
  border-radius: var(--radius-md);
  font-size: 14px;
  background: var(--color-primary-bg);
  transition: border-color var(--transition-fast);
}

.search-input:focus {
  outline: none;
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px var(--color-primary-light);
}

.search-icon {
  position: absolute;
  left: var(--spacing-md);
  top: 50%;
  transform: translateY(-50%);
  color: var(--color-text-muted);
  pointer-events: none;
}

.filter-group {
  display: flex;
  gap: var(--spacing-xs);
  background: var(--color-primary-bg);
  padding: 2px;
  border-radius: var(--radius-md);
  border: 1px solid var(--color-border-light);
}

.filter-btn {
  padding: 6px 12px;
  border: none;
  background: transparent;
  color: var(--color-text-secondary);
  font-size: 12px;
  font-weight: 600;
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: all var(--transition-fast);
}

.filter-btn:hover {
  background: var(--color-tertiary-bg);
  color: var(--color-text-primary);
}

.filter-btn.active {
  background: var(--color-primary);
  color: var(--color-text-inverse);
}

.sort-select {
  height: 36px;
  padding: 0 var(--spacing-md);
  border: 1px solid var(--color-border-medium);
  border-radius: var(--radius-md);
  font-size: 13px;
  background: var(--color-primary-bg);
  cursor: pointer;
  transition: border-color var(--transition-fast);
}

.sort-select:focus {
  outline: none;
  border-color: var(--color-primary);
}

.table-container {
  overflow-x: auto;
  overflow-y: visible;
}

.player-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
}

.table-header th {
  padding: 12px 8px;
  text-align: left;
  font-weight: 600;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-text-secondary);
  background: var(--color-secondary-bg);
  border-bottom: 2px solid var(--color-border-medium);
  white-space: nowrap;
  position: sticky;
  top: 0;
  z-index: var(--z-sticky);
}

.table-header th.sortable {
  cursor: pointer;
  user-select: none;
}

.table-header th.sortable:hover {
  color: var(--color-text-primary);
  background: var(--color-tertiary-bg);
}

.sort-icon {
  display: inline-block;
  vertical-align: middle;
  margin-left: 4px;
  opacity: 0.4;
  transition: opacity var(--transition-fast);
}

.table-header th.sortable:hover .sort-icon {
  opacity: 1;
}

.player-row {
  transition: background-color var(--transition-fast);
}

.player-row:hover {
  background-color: var(--color-tertiary-bg);
}

.player-row[data-status="drafted"] {
  opacity: 0.6;
}

.player-row[data-status="drafted"]:hover {
  background-color: transparent;
}

.player-row td {
  padding: 12px 8px;
  border-bottom: 1px solid var(--color-divider);
  vertical-align: middle;
}

/* === КОЛОНКИ === */
.col-status {
  width: 32px;
  text-align: center;
}

.status-dot {
  display: inline-block;
  width: 8px;
  height: 8px;
  border-radius: var(--radius-round);
}

.status-available { background: var(--color-success); }
.status-drafted { background: var(--color-text-muted); }

.col-player {
  min-width: 180px;
}

.player-info {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.player-name {
  font-weight: 500;
  color: var(--color-text-primary);
}

.player-meta {
  font-size: 11px;
  color: var(--color-text-muted);
}

.col-position {
  width: 50px;
  text-align: center;
}

.position-badge {
  display: inline-block;
  padding: 2px 6px;
  border-radius: var(--radius-sm);
  font-size: 10px;
  font-weight: 700;
  letter-spacing: 0.5px;
}

.position-c { background: #E3F2FD; color: #1976D2; }
.position-lw { background: #F3E5F5; color: #7B1FA2; }
.position-rw { background: #FCE4EC; color: #C2185B; }
.position-d { background: #FFF3E0; color: #F57C00; }
.position-g { background: #E8F5E9; color: #388E3C; }

.col-team {
  width: 70px;
}

.team-abbr {
  font-weight: 600;
  color: var(--color-text-secondary);
  font-size: 12px;
}

.col-salary {
  min-width: 120px;
  text-align: right;
}

.salary-value {
  font-weight: 600;
  color: var(--color-primary);
  font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
  font-size: 13px;
}

.col-stat {
  width: 50px;
  text-align: center;
  font-weight: 500;
  color: var(--color-text-secondary);
}

.col-action {
  width: 120px;
  text-align: right;
  padding-right: var(--spacing-md);
}

.btn-draft {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 6px 12px;
  border: none;
  border-radius: var(--radius-md);
  font-size: 12px;
  font-weight: 600;
  cursor: pointer;
  transition: all var(--transition-fast);
  white-space: nowrap;
}

.btn-primary {
  background: var(--color-primary);
  color: var(--color-text-inverse);
}

.btn-primary:hover {
  background: var(--color-primary-hover);
  transform: translateY(-1px);
  box-shadow: var(--shadow-sm);
}

.btn-primary:active {
  transform: translateY(0);
}

.btn-primary:disabled {
  background: var(--color-text-muted);
  cursor: not-allowed;
  opacity: 0.5;
}

.drafted-by {
  display: flex;
  align-items: center;
  gap: 4px;
  font-size: 11px;
  color: var(--color-text-muted);
}
```


### 2. SIDEBAR КОМАНДЫ

```html
<aside class="team-sidebar">
  <div class="sidebar-card">
    <!-- Header -->
    <div class="sidebar-header">
      <h3 class="sidebar-title">Ваша команда</h3>
      <div class="team-logo">
        <img src="logo.png" alt="Team Logo" width="32" height="32">
      </div>
    </div>
    
    <!-- Salary Cap Progress -->
    <div class="salary-cap-widget">
      <div class="salary-cap-header">
        <span class="label">Salary Cap</span>
        <span class="percentage">47.6%</span>
      </div>
      <div class="salary-cap-bar">
        <div class="salary-cap-fill" style="width: 47.6%"></div>
      </div>
      <div class="salary-cap-values">
        <span class="used">$45,500,000</span>
        <span class="total">/ $95,500,000</span>
      </div>
      <div class="salary-cap-remaining">
        <strong>$50,000,000</strong> осталось
      </div>
    </div>
    
    <!-- Roster Slots -->
    <div class="roster-slots">
      <div class="slot-header">
        <span>Состав</span>
        <span class="slot-count">3/6</span>
      </div>
      
      <!-- Filled slot -->
      <div class="roster-slot filled">
        <div class="slot-position">C</div>
        <div class="slot-player">
          <div class="slot-player-name">Connor McDavid</div>
          <div class="slot-player-salary">$12,500,000</div>
        </div>
        <button class="slot-remove" aria-label="Удалить">
          <svg width="14" height="14"><use href="#icon-close"></use></svg>
        </button>
      </div>
      
      <!-- Empty slot -->
      <div class="roster-slot empty">
        <div class="slot-position">LW</div>
        <div class="slot-placeholder">Выберите игрока</div>
      </div>
    </div>
  </div>
</aside>
```

```css
/* === SIDEBAR === */
.team-sidebar {
  width: 320px;
  position: sticky;
  top: var(--spacing-lg);
  align-self: flex-start;
}

.sidebar-card {
  background: var(--color-primary-bg);
  border: 1px solid var(--color-border-medium);
  border-radius: var(--radius-lg);
  overflow: hidden;
}

.sidebar-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--spacing-lg);
  border-bottom: 1px solid var(--color-border-light);
}

.sidebar-title {
  margin: 0;
  font-size: 16px;
  font-weight: 600;
}

.team-logo img {
  display: block;
  border-radius: var(--radius-sm);
}

/* === SALARY CAP WIDGET === */
.salary-cap-widget {
  padding: var(--spacing-lg);
  background: linear-gradient(135deg, #F8F9FA 0%, #FFFFFF 100%);
  border-bottom: 1px solid var(--color-border-light);
}

.salary-cap-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spacing-sm);
}

.salary-cap-header .label {
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-text-secondary);
}

.salary-cap-header .percentage {
  font-size: 14px;
  font-weight: 700;
  color: var(--color-primary);
}

.salary-cap-bar {
  height: 8px;
  background: var(--color-border-light);
  border-radius: var(--radius-lg);
  overflow: hidden;
  margin-bottom: var(--spacing-sm);
}

.salary-cap-fill {
  height: 100%;
  background: linear-gradient(90deg, var(--color-primary) 0%, var(--color-secondary) 100%);
  transition: width var(--transition-slow);
  border-radius: var(--radius-lg);
}

.salary-cap-fill[style*="width: 7"],
.salary-cap-fill[style*="width: 8"],
.salary-cap-fill[style*="width: 9"] {
  background: linear-gradient(90deg, var(--color-warning) 0%, var(--color-danger) 100%);
}

.salary-cap-values {
  display: flex;
  gap: 4px;
  font-size: 13px;
  margin-bottom: 4px;
}

.salary-cap-values .used {
  font-weight: 700;
  font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
  color: var(--color-text-primary);
}

.salary-cap-values .total {
  color: var(--color-text-muted);
  font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
}

.salary-cap-remaining {
  font-size: 11px;
  color: var(--color-text-secondary);
}

.salary-cap-remaining strong {
  color: var(--color-success);
  font-weight: 700;
}

/* === ROSTER SLOTS === */
.roster-slots {
  padding: var(--spacing-lg);
}

.slot-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spacing-md);
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-text-secondary);
}

.slot-count {
  color: var(--color-primary);
}

.roster-slot {
  display: flex;
  align-items: center;
  gap: var(--spacing-md);
  padding: var(--spacing-md);
  margin-bottom: var(--spacing-sm);
  border: 1px solid var(--color-border-light);
  border-radius: var(--radius-md);
  transition: all var(--transition-fast);
}

.roster-slot.empty {
  border-style: dashed;
  background: var(--color-secondary-bg);
}

.roster-slot.filled {
  background: var(--color-primary-bg);
}

.roster-slot.filled:hover {
  border-color: var(--color-border-dark);
  box-shadow: var(--shadow-sm);
}

.slot-position {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 32px;
  height: 32px;
  background: var(--color-secondary-bg);
  border-radius: var(--radius-sm);
  font-size: 11px;
  font-weight: 700;
  color: var(--color-text-secondary);
  flex-shrink: 0;
}

.roster-slot.filled .slot-position {
  background: var(--color-primary-light);
  color: var(--color-primary);
}

.slot-player {
  flex: 1;
  min-width: 0;
}

.slot-player-name {
  font-size: 13px;
  font-weight: 500;
  color: var(--color-text-primary);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.slot-player-salary {
  font-size: 11px;
  font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
  color: var(--color-text-muted);
}

.slot-placeholder {
  font-size: 12px;
  color: var(--color-text-muted);
  font-style: italic;
}

.slot-remove {
  width: 24px;
  height: 24px;
  display: flex;
  align-items: center;
  justify-content: center;
  border: none;
  background: transparent;
  color: var(--color-text-muted);
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: all var(--transition-fast);
  flex-shrink: 0;
}

.slot-remove:hover {
  background: var(--color-danger-light);
  color: var(--color-danger);
}
```


### 3. DRAFT STATUS BAR

```html
<div class="draft-status-bar">
  <div class="container">
    <div class="status-content">
      <!-- Timer -->
      <div class="status-item timer-widget">
        <svg class="timer-icon" width="20" height="20">
          <use href="#icon-clock"></use>
        </svg>
        <div class="timer-display">
          <span class="timer-value">0:47</span>
          <span class="timer-label">до автопика</span>
        </div>
      </div>
      
      <!-- Current Pick -->
      <div class="status-item current-pick">
        <div class="pick-indicator">
          <span class="pick-number">Пик #12</span>
          <span class="pick-round">Раунд 2</span>
        </div>
        <div class="pick-user">
          <div class="user-avatar">JD</div>
          <div class="user-info">
            <span class="user-name">John Doe</span>
            <span class="user-team">Team Alpha</span>
          </div>
        </div>
      </div>
      
      <!-- Next Pick -->
      <div class="status-item next-pick">
        <span class="next-label">Следующий:</span>
        <span class="next-user">Sarah Wilson</span>
      </div>
      
      <!-- Draft Progress -->
      <div class="status-item draft-progress">
        <div class="progress-bar">
          <div class="progress-fill" style="width: 33%"></div>
        </div>
        <span class="progress-text">20/60 пиков</span>
      </div>
    </div>
  </div>
</div>
```

```css
/* === DRAFT STATUS BAR === */
.draft-status-bar {
  position: sticky;
  top: 0;
  z-index: var(--z-sticky);
  background: var(--color-header-bg);
  color: var(--color-text-inverse);
  padding: var(--spacing-md) 0;
  box-shadow: var(--shadow-md);
}

.status-content {
  display: flex;
  align-items: center;
  gap: var(--spacing-xl);
  flex-wrap: wrap;
}

.status-item {
  display: flex;
  align-items: center;
  gap: var(--spacing-md);
}

/* === TIMER === */
.timer-widget {
  padding: var(--spacing-sm) var(--spacing-md);
  background: rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-md);
}

.timer-icon {
  color: var(--color-warning);
}

.timer-display {
  display: flex;
  flex-direction: column;
}

.timer-value {
  font-size: 18px;
  font-weight: 700;
  font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
  line-height: 1;
}

.timer-label {
  font-size: 10px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  opacity: 0.7;
}

.timer-widget[data-warning="true"] {
  background: var(--color-danger);
  animation: pulse 1s infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.8; }
}

/* === CURRENT PICK === */
.current-pick {
  flex: 1;
  min-width: 200px;
}

.pick-indicator {
  display: flex;
  flex-direction: column;
  margin-right: var(--spacing-md);
}

.pick-number {
  font-size: 14px;
  font-weight: 700;
}

.pick-round {
  font-size: 10px;
  opacity: 0.7;
}

.pick-user {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
}

.user-avatar {
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--color-primary);
  border-radius: var(--radius-round);
  font-size: 12px;
  font-weight: 700;
}

.user-info {
  display: flex;
  flex-direction: column;
}

.user-name {
  font-size: 13px;
  font-weight: 600;
}

.user-team {
  font-size: 11px;
  opacity: 0.7;
}

/* === NEXT PICK === */
.next-pick {
  padding-left: var(--spacing-xl);
  border-left: 1px solid rgba(255, 255, 255, 0.2);
}

.next-label {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  opacity: 0.7;
}

.next-user {
  font-size: 13px;
  font-weight: 600;
}

/* === PROGRESS === */
.draft-progress {
  margin-left: auto;
}

.progress-bar {
  width: 120px;
  height: 6px;
  background: rgba(255, 255, 255, 0.2);
  border-radius: var(--radius-lg);
  overflow: hidden;
}

.progress-fill {
  height: 100%;
  background: var(--color-success);
  transition: width var(--transition-slow);
  border-radius: var(--radius-lg);
}

.progress-text {
  font-size: 11px;
  font-weight: 600;
  margin-left: var(--spacing-sm);
}
```


### 4. LEADERBOARD TABLE

```html
<div class="leaderboard-container">
  <h2 class="leaderboard-title">Таблица лидеров</h2>
  
  <table class="leaderboard-table">
    <thead>
      <tr>
        <th class="col-rank">#</th>
        <th class="col-team">Команда</th>
        <th class="col-owner">Владелец</th>
        <th class="col-stat">Очки</th>
        <th class="col-stat">Недель</th>
        <th class="col-salary">Total Salary</th>
        <th class="col-action"></th>
      </tr>
    </thead>
    <tbody>
      <tr class="leaderboard-row rank-1">
        <td class="col-rank">
          <div class="rank-badge">1</div>
        </td>
        <td class="col-team">
          <div class="team-cell">
            <img src="logo.png" class="team-logo-sm" alt="">
            <span class="team-name">Ice Breakers</span>
          </div>
        </td>
        <td class="col-owner">John Doe</td>
        <td class="col-stat"><strong>458</strong></td>
        <td class="col-stat">3</td>
        <td class="col-salary">$94,250,000</td>
        <td class="col-action">
          <button class="btn-view-team">Состав</button>
        </td>
      </tr>
    </tbody>
  </table>
</div>
```

```css
/* === LEADERBOARD === */
.leaderboard-container {
  background: var(--color-primary-bg);
  border: 1px solid var(--color-border-medium);
  border-radius: var(--radius-lg);
  padding: var(--spacing-xl);
}

.leaderboard-title {
  margin: 0 0 var(--spacing-lg) 0;
  font-size: 22px;
  font-weight: 600;
}

.leaderboard-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 13px;
}

.leaderboard-table thead th {
  padding: 12px 8px;
  text-align: left;
  font-weight: 600;
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  color: var(--color-text-secondary);
  border-bottom: 2px solid var(--color-border-medium);
}

.leaderboard-row td {
  padding: 16px 8px;
  border-bottom: 1px solid var(--color-divider);
}

.leaderboard-row:hover {
  background: var(--color-tertiary-bg);
}

.col-rank {
  width: 60px;
}

.rank-badge {
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-round);
  font-weight: 700;
  font-size: 14px;
  background: var(--color-secondary-bg);
  color: var(--color-text-secondary);
}

.rank-1 .rank-badge {
  background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
  color: #FFFFFF;
  box-shadow: 0 2px 8px rgba(255, 215, 0, 0.3);
}

.rank-2 .rank-badge {
  background: linear-gradient(135deg, #C0C0C0 0%, #A8A8A8 100%);
  color: #FFFFFF;
}

.rank-3 .rank-badge {
  background: linear-gradient(135deg, #CD7F32 0%, #B87333 100%);
  color: #FFFFFF;
}

.team-cell {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
}

.team-logo-sm {
  width: 24px;
  height: 24px;
  border-radius: var(--radius-sm);
}

.team-name {
  font-weight: 600;
  color: var(--color-text-primary);
}

.btn-view-team {
  padding: 4px 12px;
  border: 1px solid var(--color-border-medium);
  background: transparent;
  color: var(--color-text-secondary);
  border-radius: var(--radius-sm);
  font-size: 11px;
  font-weight: 600;
  cursor: pointer;
  transition: all var(--transition-fast);
}

.btn-view-team:hover {
  border-color: var(--color-primary);
  color: var(--color-primary);
  background: var(--color-primary-light);
}
```


### 5. RESPONSIVE BREAKPOINTS

```css
/* === АДАПТИВНОСТЬ === */
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 var(--spacing-lg);
}

/* Desktop Large (1440px+) */
@media (min-width: 1440px) {
  .container {
    max-width: 1320px;
  }
}

/* Tablet (768px - 1023px) */
@media (max-width: 1023px) {
  .draft-layout {
    grid-template-columns: 1fr;
  }
  
  .team-sidebar {
    width: 100%;
    position: static;
    order: -1;
    margin-bottom: var(--spacing-xl);
  }
  
  .table-controls {
    flex-direction: column;
    align-items: stretch;
  }
  
  .search-box {
    width: 100%;
  }
}

/* Mobile (< 768px) */
@media (max-width: 767px) {
  :root {
    --spacing-lg: 12px;
    --spacing-xl: 16px;
  }
  
  .player-table {
    font-size: 12px;
  }
  
  .player-table th,
  .player-table td {
    padding: 8px 4px;
  }
  
  /* Скрываем менее важные колонки */
  .col-team,
  .col-stat:nth-child(n+7) {
    display: none;
  }
  
  .col-player {
    min-width: 140px;
  }
  
  .status-content {
    flex-direction: column;
    align-items: stretch;
    gap: var(--spacing-md);
  }
  
  .status-item {
    width: 100%;
  }
  
  .draft-progress {
    margin-left: 0;
  }
  
  .btn-draft {
    padding: 4px 8px;
    font-size: 11px;
  }
}

/* Mobile Small (< 480px) */
@media (max-width: 479px) {
  .player-name {
    font-size: 12px;
  }
  
  .player-meta {
    display: none;
  }
  
  .salary-value {
    font-size: 11px;
  }
}
```


### 6. UTILITY CLASSES

```css
/* === УТИЛИТЫ === */
.hidden { display: none !important; }
.invisible { visibility: hidden !important; }

.flex { display: flex; }
.flex-col { flex-direction: column; }
.items-center { align-items: center; }
.justify-between { justify-content: space-between; }
.gap-sm { gap: var(--spacing-sm); }
.gap-md { gap: var(--spacing-md); }
.gap-lg { gap: var(--spacing-lg); }

.mt-sm { margin-top: var(--spacing-sm); }
.mt-md { margin-top: var(--spacing-md); }
.mt-lg { margin-top: var(--spacing-lg); }
.mb-sm { margin-bottom: var(--spacing-sm); }
.mb-md { margin-bottom: var(--spacing-md); }
.mb-lg { margin-bottom: var(--spacing-lg); }

.p-sm { padding: var(--spacing-sm); }
.p-md { padding: var(--spacing-md); }
.p-lg { padding: var(--spacing-lg); }

.rounded-sm { border-radius: var(--radius-sm); }
.rounded-md { border-radius: var(--radius-md); }
.rounded-lg { border-radius: var(--radius-lg); }

.shadow-sm { box-shadow: var(--shadow-sm); }
.shadow-md { box-shadow: var(--shadow-md); }
.shadow-lg { box-shadow: var(--shadow-lg); }
```


***

## 🎯 SVG ИКОНКИ (SPRITE)

```html
<svg style="display: none;">
  <symbol id="icon-search" viewBox="0 0 16 16">
    <path fill="currentColor" d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"/>
  </symbol>
  
  <symbol id="icon-sort" viewBox="0 0 12 12">
    <path fill="currentColor" d="M6 0l4 5H2l4-5zm0 12l4-5H2l4 5z"/>
  </symbol>
  
  <symbol id="icon-plus" viewBox="0 0 14 14">
    <path fill="currentColor" d="M7 0a1 1 0 0 1 1 1v5h5a1 1 0 1 1 0 2H8v5a1 1 0 1 1-2 0V8H1a1 1 0 0 1 0-2h5V1a1 1 0 0 1 1-1z"/>
  </symbol>
  
  <symbol id="icon-close" viewBox="0 0 14 14">
    <path fill="currentColor" d="M13.7.3c.4.4.4 1 0 1.4L8.4 7l5.3 5.3c.4.4.4 1 0 1.4-.4.4-1 .4-1.4 0L7 8.4l-5.3 5.3c-.4.4-1 .4-1.4 0-.4-.4-.4-1 0-1.4L5.6 7 .3 1.7C-.1 1.3-.1.7.3.3c.4-.4 1-.4 1.4 0L7 5.6 12.3.3c.4-.4 1-.4 1.4 0z"/>
  </symbol>
  
  <symbol id="icon-lock" viewBox="0 0 12 12">
    <path fill="currentColor" d="M6 0a3 3 0 0 1 3 3v2h1a1 1 0 0 1 1 1v5a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V6a1 1 0 0 1 1-1h1V3a3 3 0 0 1 3-3zm0 1a2 2 0 0 0-2 2v2h4V3a2 2 0 0 0-2-2z"/>
  </symbol>
  
  <symbol id="icon-clock" viewBox="0 0 20 20">
    <path fill="currentColor" d="M10 0a10 10 0 1 1 0 20 10 10 0 0 1 0-20zm0 2a8 8 0 1 0 0 16 8 8 0 0 0 0-16zm1 3v5.59l3.95 2.37-.9 1.5L9 11.59V5h2z"/>
  </symbol>
</svg>
```


***

## ⚡ JAVASCRIPT ИНТЕГРАЦИИ

```javascript
// Инициализация таблицы с сортировкой
class PlayerTable {
  constructor(selector) {
    this.table = document.querySelector(selector);
    this.data = [];
    this.filteredData = [];
    this.sortColumn = null;
    this.sortDirection = 'asc';
    
    this.init();
  }
  
  init() {
    this.bindEvents();
  }
  
  bindEvents() {
    // Сортировка
    this.table.querySelectorAll('.sortable').forEach(th => {
      th.addEventListener('click', () => {
        const column = th.dataset.sort;
        this.sort(column);
      });
    });
    
    // Фильтры позиций
    document.querySelectorAll('.filter-btn').forEach(btn => {
      btn.addEventListener('click', (e) => {
        document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
        e.target.classList.add('active');
        this.filterByPosition(e.target.dataset.position);
      });
    });
    
    // Поиск
    document.querySelector('.search-input').addEventListener('input', (e) => {
      this.search(e.target.value);
    });
  }
  
  sort(column) {
    if (this.sortColumn === column) {
      this.sortDirection = this.sortDirection === 'asc' ? 'desc' : 'asc';
    } else {
      this.sortColumn = column;
      this.sortDirection = 'asc';
    }
    
    this.render();
  }
  
  filterByPosition(position) {
    if (position === 'all') {
      this.filteredData = [...this.data];
    } else {
      this.filteredData = this.data.filter(p => p.position === position);
    }
    this.render();
  }
  
  search(query) {
    const q = query.toLowerCase();
    this.filteredData = this.data.filter(p => 
      p.name.toLowerCase().includes(q)
    );
    this.render();
  }
  
  render() {
    // Render logic here
  }
}

// Salary Cap tracker
class SalaryCapTracker {
  constructor(cap = 95500000) {
    this.cap = cap;
    this.used = 0;
    this.update();
  }
  
  add(salary) {
    if (this.used + salary > this.cap) {
      return false;
    }
    this.used += salary;
    this.update();
    return true;
  }
  
  remove(salary) {
    this.used -= salary;
    this.update();
  }
  
  update() {
    const percentage = (this.used / this.cap * 100).toFixed(1);
    document.querySelector('.percentage').textContent = `${percentage}%`;
    document.querySelector('.salary-cap-fill').style.width = `${percentage}%`;
    document.querySelector('.used').textContent = this.formatMoney(this.used);
    document.querySelector('.remaining strong').textContent = this.formatMoney(this.cap - this.used);
  }
  
  formatMoney(amount) {
    return `$${amount.toLocaleString('en-US')}`;
  }
}

// Draft Timer
class DraftTimer {
  constructor(duration = 60) {
    this.duration = duration;
    this.remaining = duration;
    this.interval = null;
    this.callbacks = [];
  }
  
  start() {
    this.interval = setInterval(() => {
      this.remaining--;
      this.update();
      
      if (this.remaining <= 0) {
        this.stop();
        this.onTimeout();
      }
    }, 1000);
  }
  
  stop() {
    clearInterval(this.interval);
  }
  
  reset() {
    this.stop();
    this.remaining = this.duration;
    this.update();
  }
  
  update() {
    const minutes = Math.floor(this.remaining / 60);
    const seconds = this.remaining % 60;
    const display = `${minutes}:${seconds.toString().padStart(2, '0')}`;
    
    document.querySelector('.timer-value').textContent = display;
    
    if (this.remaining <= 10) {
      document.querySelector('.timer-widget').dataset.warning = 'true';
    }
  }
  
  onTimeout() {
    // Auto-pick logic
    this.callbacks.forEach(cb => cb());
  }
  
  addCallback(callback) {
    this.callbacks.push(callback);
  }
}
```


***

## 📱 ACCESSIBILITY

```css
/* === ФОКУС-СТИЛИ === */
*:focus {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

*:focus:not(:focus-visible) {
  outline: none;
}

*:focus-visible {
  outline: 2px solid var(--color-primary);
  outline-offset: 2px;
}

/* === ВЫСОКИЙ КОНТРАСТ === */
@media (prefers-contrast: high) {
  :root {
    --color-text-secondary: #495057;
    --color-border-light: #ADB5BD;
  }
}

/* === PREFERS REDUCED MOTION === */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}

/* === SCREEN READER ONLY === */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}
```


***

## 🎉 ИТОГ

Этот промпт содержит:

- ✅ Полную цветовую палитру с точными HEX кодами
- ✅ Детальную типографическую систему
- ✅ Готовые HTML/CSS компоненты для всех ключевых элементов
- ✅ Адаптивные брейкпоинты
- ✅ JavaScript логику для интерактивности
- ✅ SVG иконки
- ✅ Accessibility стандарты
- ✅ Utility классы

**Этот промпт можно напрямую передать ИИ-разработчику или использовать как полную спецификацию для manual coding.**

